# Changelog

本專案的所有顯著變更將記錄在此檔案中。
格式參考自 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/)。

## [1.1.0] - 2026-01

### Added
- **中斷保護機制**：新增對 `KeyboardInterrupt` (Ctrl+C) 的捕捉處理，確保手動中斷時仍能執行存檔邏輯。
- **自動路徑識別**：引入 `os.path` 邏輯，自動獲取使用者的桌面路徑（Desktop）作為預設輸出位置。
- **底部重試邏輯**：新增 `MAX_BOTTOM_RETRIES` 參數，強化對頁面載入延遲的容錯能力。

### Changed
- **優化捲動策略**：將原本的一次性大跨度捲動改為「碎步式捲動」(`SCROLL_STEP`)，提升動態內容觸發的成功率。
- **存檔邏輯優化**：無論是正常結束或中斷，均會執行最後的名單排序與寫入。

## [1.0.0] - 2026-01

### Added
- **初始版本發佈**：實現基於 Selenium 的 X 名單抓取功能。
- **半自動登入機制**：整合手動登入與自動執行邏輯，提升爬蟲存活率。
- **智慧捲動偵測**：加入高度檢測機制，能自動判斷是否到達頁面底部。
- **重複過濾系統**：使用 `set()` 結構確保輸出的名單不包含重複帳號。
- **反偵測優化**：預設加入 `disable-blink-features` 設定以避開基本的自動化檢查。